# OS / editor
.DS_Store
Thumbs.db

# Dependencies
node_modules/

# Logs
*.log

# Env / secrets (never commit)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa*
.DS_Store

# CDK / build
cdk.out/

# Local debug artifacts (do not commit)
infra/cdk/ddb-key.json
infra/cdk/entitlements-backup.json
infra/cdk/idem-logs.json

# Local archives (do not commit)
services.zip
replace.txt

# Local CLI captures (do not commit)
tmp_alarms.json

# Accidental local artifact
infra/cdk/git

# Personal progress logs (kept local unless explicitly shared)
.a_PROGRESS/

# Private local Codex contract (do not commit)
CHAT/CODEX.private.md
